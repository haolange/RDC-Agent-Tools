# rdx-tools 文档

本目录记录独立分发的 `rdx-tools` 包。

- 入口（entry points）：`mcp/run_mcp.py`、`cli/run_cli.py`、`rdx.bat`
- 运行时二进制（runtime binaries）：`binaries/windows/x64` 与 `binaries/windows/x64/pymodules`
- tool catalog：`spec/tool_catalog_196.json`